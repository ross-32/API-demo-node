# Aster Builder Code Demo Environment Variables
# 环境变量配置示例

# API Host / API 服务地址
HOST=http://10.100.7.198:9081

# User Account / 用户账户
USER_ADDRESS=0x9FdC25AE9dE3791ECb122959ff196d600d5aDDA4

# Main Private Key (for main=True authorization operations only) / 主钱包私钥（仅用于 main=True 授权类接口）
MAIN_PRIVATE_KEY=0x...your_main_private_key...

# Signer/Agent Address / Signer/Agent 地址
SIGNER_ADDRESS=0x34b208e2674f120343B541e4C75C9035EbF8f5c4

# Signer Private Key (for trading operations) / Signer 私钥（用于交易类接口）
SIGNER_PRIVATE_KEY=0x...your_signer_private_key...

# Builder Address / Builder 地址
BUILDER_ADDRESS=0xc2af13e1B1de3A015252A115309A0F9DEEDCFa0A

# Builder Name / Builder 名称
BUILDER_NAME=ivan

# Max Fee Rate (as string) / 最大费率（字符串格式）
MAX_FEE_RATE=0.00001

# Fee Rate for orders (as string) / 订单费率（字符串格式）
FEE_RATE=0.00001

# Agent Name / Agent 名称
AGENT_NAME=2dkkd0001

# IP Whitelist (optional, comma-separated) / IP 白名单（可选，逗号分隔）
IP_WHITELIST=127.0.0.9

# Agent Expiry Timestamp (milliseconds) / Agent 过期时间戳（毫秒）
AGENT_EXPIRED=1867945395040

# Chain ID for EIP-712 signature / EIP-712 签名的 Chain ID
CHAIN_ID=56

# Aster Chain Environment / Aster 链环境
ASTER_CHAIN=Testnet
